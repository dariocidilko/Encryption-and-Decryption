This is a program that will let you encrypt and decrypt files in your folder of choosing.
If you use Windows, you can run the script to automatically download Python and the cryptography library.
If you use Arch Linux, you can download Python and the cryptography library by running this:
  - sudo pacman -S Python python-cryptography
